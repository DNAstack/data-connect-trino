name: Build, Test & Analyse

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test-java-app:
    name: Build & Test Java App
    uses: DNAstack/dnastack-development-tools/.github/workflows/build-test-java-app.yml@63fe9b316d777c9780bf7a786f5b744a4af4f606
    with:
      java-version: 21
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}

  sast:
    name: SAST (SonarQube)
    uses: DNAstack/dnastack-development-tools/.github/workflows/sast.yml@63fe9b316d777c9780bf7a786f5b744a4af4f606
    with:
      with-frontend: false
      java-version: 21
      code-coverage-enabled: true
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}
      sonar-token: ${{ secrets.SONAR_TOKEN }}
      sonar-host-url: ${{ secrets.SONAR_HOST_URL }}

  sca:
    name: SCA (Trivy)
    uses: DNAstack/dnastack-development-tools/.github/workflows/sca.yml@63fe9b316d777c9780bf7a786f5b744a4af4f606
    with:
      java-version: 21
    secrets:
      pat-with-read-packages-permission: ${{ secrets.AUTH_TOKEN }}

  secrets-detection:
    name: Secrets Detection (Gitleaks)
    uses: DNAstack/dnastack-development-tools/.github/workflows/secrets-detection.yml@63fe9b316d777c9780bf7a786f5b744a4af4f606
    secrets:
      gitleaks-license: ${{ secrets.GITLEAKS_LICENSE }}
